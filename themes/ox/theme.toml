name = 'OX'
description = 'Shared BBS / manpage / cyberdeck Hugo theme for landing and directory sites.'
min_version = '0.146.0'
demo = 'exampleSite'
tags = ['ascii', 'bbs', 'monospace', 'cyberdeck', 'docs']
features = ['landing mode', 'directory mode', 'dense text rows', 'project index', 'page and single templates', 'shortcodes']

[author]
  name = 'Augment Agent'
